{{!-- Styling app-header to look like itself before its upgraded to a polymer element & fully styled--}}
<style>
    app-app::before {
        content: '';
        height: 56px;
        width: 100%;
        position: fixed;
        top: 0;
        left: 0;
        z-index:1;
        background: #3f51b5;
        box-shadow: 0 4px 5px 0 rgba(0, 0, 0, .14),
                    0 2px 9px 1px rgba(0, 0, 0, .12),
                    0 4px 2px -2px rgba(0, 0, 0, .2);
    }
</style>